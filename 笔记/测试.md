







1、( 2分 ) 下列不属于计算机硬件的是（）已提交

内存

操作系统

CPU

I/O设备

2、( 2分 ) 下列不是邮件服务相关协议的是（）已提交

POP3

FTP

IMAP

SMTP

3、( 2分 ) 用来查看域名解析的命令是（）已提交

nslookup

ipconfig

dnsd

dslookup

4、( 2分 ) 以下选项哪一项对于net user命令描述是错误的已提交

net user 可以查询用户和修改密码和活动状态

net user 可以使电脑关机

net user可以新建用户和删除用户

net user 可以把普通用户提升为管理员权限

5、( 2分 ) Web服务器Apache默认的接听连接端口号为（）已提交

80

82

8080

1024

6、( 2分 ) Windows下查找被80端口占用的进程PID（）已提交

netstat –ano | find“80”

netstat –ano | find 80

netstat –ano | grep 80

netstat –anp | grep 80

7、( 2分 ) 以下组中不是windows默认拥有的是已提交

users

administrators

guests

windows

8、( 2分 ) Windows中，什么命令可以查看系统相关信息已提交

ping

netstat

systeminfo

ipconfig

9、( 2分 ) 什么是Linux？已提交

Linux是一个操作系统

Linux是一个编程软件

Linux是一个绘图软件

Linux是一门编程语言

10、( 2分 ) telnet默认端口号是已提交

21

23

20

22

11、( 2分 ) （ ）命令能显示本机所有网络适配器的详细信息。已提交

ipconfig

ping

showip

ipconfig /all

12、( 2分 ) 怎样显示当前目录（ )已提交

cd

who

ls

pwd

13、( 2分 ) Linux安装时说法错误的是？已提交

Linux安装可以选择安装中文

Linux安装需要创建root密码

可以安装Linux图形化界面

Linux没有图形化界面

14、( 2分 ) 文件rwx权限分别是什么含义？已提交

读、写、执行

读、执行、写

执行、写、读

执行、读、写

15、( 2分 ) Linux中root用户的UID和GID分别为多少？已提交

UID=0，GID=0

UID=1，GID=0

UID=1，GID=1

UID=0，GID=1

16、( 2分 ) 以下说法正确的是？已提交

VIM只能编写txt文件

利用VIM可以创建目录

windows下也有VIM编辑器

VIM是Linux下的编辑器

17、( 2分 ) 哪个命令可以将普通用户转换成超级用户（）已提交

super

passwd

tar

su

18、( 2分 ) 在创建Linxu分区时，一定要创建（）两个分区已提交

FAT/NTFS

FAT/SWAP

NTFS/SWAP

SWAP/根分区

19、( 2分 ) Linux中如何查看历史命令已提交

whoami

cat /etc/passwd

cat /etc/shadow

history

20、( 2分 ) 用rm命令删除非空目录需要加上哪个参数？（ ）已提交

c

r

t

f

21、( 2分 ) 在http响应中，状态码404表示已提交

服务器请求成功

请求需要通过身份验证或授权

服务器无法找到请求指定的资源

请求消息中存在语法错误

22、( 2分 ) 中间件是（）已提交

资源管理器

独立的系统软件

数据库

集成资源

23、( 2分 ) 中间件的特性是已提交

消息传输的完整性

易用性

以上都包括

位置透明性

24、( 2分 ) access.log是Apache的（）日志已提交

访问

安装

登录

错误

25、( 2分 ) 以下不属于中间件的是已提交

postgresql

tomcat

nginx

jboss

26、( 2分 ) 创建数据库的语法格式是：已提交

USE 数据库名；

SHOW DATABASE；

DROP DATABASE 数据库名；

CREATE DATABASE 数据库名；

27、( 2分 ) 以下哪个命令用于删除数据库？已提交

drop table

DROP DATABASE

DELETE DATABASE

DELETE

28、( 2分 ) "公司中有多个部门和多名职员，每个职员只能属于一个部门，一个部门可以有多名职员， 从职员到部门的联系类型是"已提交

多对一

多对多

一对一

一对多

29、( 2分 ) select version();返回的结果是已提交

当前的用户

当前数据库版本号

所有数据表

所有数据库

30、( 2分 ) varchar和char的区别是什么？已提交

varchar为定长，char为变长

varchar输入的字符串长度严格受限，太长或太短都会报错；char只要不长于限制就不会报错

varchar为变长，char为定长

char输入的字符串长度严格受限，太长或太短都会报错；varchar只要不长于限制就不会报错

31、( 2分 ) 在存在下列关键字的SQL语句中，不可能出现Where子句的是_____。已提交

Insert

 Delete

Alter

Update

32、( 2分 ) 如果要在一张管理职工工资的表中限制工资的输入范围，应使用_____约束。已提交

FOREIGN KEY

unique

PDRIMARY KEY

check

33、( 2分 ) 要查询XSH数据库CP表中产品名含有“冰箱”的产品情况，可用_____命令。已提交

SELECT * FROM CP WHERE 产品名称=‘冰箱’

SELECT * FROM CP WHERE 产品名称 LIKE ‘%冰箱%’

SELECT * FROM XSH WHERE 产品名称 LIKE ‘冰箱’

SELECT * FROM CP WHERE 产品名称 LIKE ‘冰箱’

34、( 2分 ) 以下哪个mysql命令可以查看数据表的结构信息_____。已提交

 select * from 表名

desc 表名

create table 表名

show tables;

35、( 2分 ) “mysql”的默认端口号是？已提交

3306

1141

1143

443

36、( 2分 ) IP 报头中包含什么信息？（）已提交

源及目的 IP 地址和 MAC 地址

只有目的 IP 地址和 MAC 地址

源和目的 MAC 地址

源和目的 IP 地址

37、( 2分 ) 访问控制列表(ACL)分为标准和扩展两种。下面关于ACL的描述中,错误的是已提交

标准ACL可以根据分组中的IP目标地址进行过滤

标准ACL可以根据分组中的IP源地址进行过滤

扩展ACL可以根据不同的上层协议信息进行过滤

扩展ACL可以根据分组中的IP目标地址进行过滤

38、( 2分 ) 在TCP/IP协议栈中，下面哪一组选项能够唯一地确定一个TCP连接()已提交

源地址、目的地址、源端口号和目的端口号

源IP地址和目的端口号

源IP地址和源端口号

目的地址和源端口号

39、( 2分 ) Telnet是TCP/IP哪一层的协议已提交

网络接口层

网际层

应用层

物理层

40、( 2分 ) 在TCP/IP协议簇中，直接为ICMP提供服务的协议是已提交

PPP

TCP

IP

UDP

41、( 2分 ) C 类 IP 地址的默认子网掩码是？已提交

255.255.255.255

255.255.0.0

255.0.0.0

255.255.255.0

42、( 2分 ) 路由器属于OSI模型的哪一层？已提交

应用层

网络层

物理层

数据链路层

43、( 2分 ) 网络通信是通过( )实现的，它们是通信双方必须遵守的约定。已提交

双绞线

调制解调器

通信协议

网卡

44、( 2分 ) 计算机网络的功能是实现（）。已提交

数据处理

信息传输与资源共享

资源独享与数据通讯

文献查询

45、( 2分 ) 文件上传中可以绕过白名单过滤并且成功执行的是已提交

文件解析漏洞

大小写混合

修改type

双写

46、( 2分 ) 下列输入弱口令的是已提交

123456qq

hiytaa！@#qqqq

password123

admin123

47、( 2分 ) 不属于信息收集的工具是已提交

sqlmap

nmap

御剑

hunter

48、( 2分 ) SQLMAP拖库的命令为已提交

down

install

dump

up

49、( 2分 ) 存储型XSS与反射型XSS和DOM型XSS最大的区别是已提交

存储型XSS不需要手动去触发

存储型XSS的影响范围更广

存储型XSS的危害更大

存储型XSS最为常见

50、( 2分 ) 下列哪项不是XSS攻击的类型已提交

cookie型

反射型

存储型

DOM型







